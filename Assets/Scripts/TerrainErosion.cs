using UnityEngine;

public class TerrainErosion : MonoBehaviour
{
    // int erosionRadius; // range (2, 8)
    // float intertia; // range (0, 1)
    // float sedimentCapacityFactor = 4; // Multiplier for how much sediment a droplet can carry
    // float minSedimentCapacity = .01f; // Used to prevent carry capacity getting too close to zero on flatter terrain
    // float erodeSpeed = .3f; // range (0, 1)
    // float depositSpeed = .3f; // range (0, 1)
    // float evaporationSpeed = .1f;  // range (0, 1)
    // float gravity;
    // int maxDropletLifetime = 30;
    // float initialWaterVolume = 1;
    // float initialSpeed = 1;

    System.Random rnd;

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
